.side-bar
  .side-bar__user
    .side-bar__user--info
      .side-bar__user--info__name
        = current_user.name
      .side-bar__user--info__icons
        = link_to new_group_path, class: "contents__btn contents__btn--edit" do 
          =icon('far', 'edit')
        = link_to edit_user_path(current_user), class: "contents__btn contents__btn--cog" do
          =icon('fas', 'cog')

  .side-bar__groups
    - current_user.groups.each do |group|
      .group
        = link_to '#' do
          .group__name
            = group.name
          .group__status
            まだメッセージがありません